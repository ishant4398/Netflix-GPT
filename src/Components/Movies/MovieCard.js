import React from "react";

const MovieCard = ({ movie }) => {
  //   console.log(movie);
  return (
    <div>
      <img src={""} alt={"Movie-Pic"}></img>
    </div>
  );
};

export default MovieCard;
